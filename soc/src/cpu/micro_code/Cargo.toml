[package]
name = "micro_code"
version = "0.1.0"
authors = ["Ramy"]
edition = "2018"

[features]
default = []
build = ["csv"]

[dependencies]
num-derive = "0.3.0"
num-traits = { version = "0.2.11", default-features = false }

csv = { version = "1.1.2", optional = true }
